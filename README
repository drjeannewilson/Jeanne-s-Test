Playing around with creating git repositories. Made a mistake by making the original 
directory here then pushing that to the repository. Should have made it anywhere else so that 
github could then clone the repository back here.
